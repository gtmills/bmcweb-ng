redfish: expand Registries and JsonSchemas from stubs to full collections

service_root.rs:
  - 5 registries advertised: Base v1.17.0, TaskEvent v1.0.3,
    ResourceEvent v1.3.0, HeartbeatEvent v1.0.1, OpenBMC v1.0.0
  - New GET /Registries/{registry_id} handler returns a
    MessageRegistryFile resource with Location → canonical DMTF URI
  - 26 JsonSchemas advertised covering all resource types implemented
    in bmcweb-ng (ServiceRoot, ComputerSystem, Chassis, Manager,
    SessionService, AccountService, EventService, TaskService,
    UpdateService, LogService, Processor, Memory, EthernetInterface,
    NetworkProtocol, Power, Thermal, Sensor, CertificateService,
    TelemetryService, and related sub-resources)
  - New GET /JsonSchemas/{schema_id} handler returns a JsonSchemaFile
    resource with Location → canonical DMTF schema URI
  - RegistryDef and SchemaDef static slice data structures replace
    the previous hard-coded single-member stubs

redfish/mod.rs:
  - Two new routes added: /Registries/:registry_id, /JsonSchemas/:schema_id

Tests:
  - test_registries_collection (count=5, Base and OpenBMC present)
  - test_get_registry_base (Id, type, URI)
  - test_get_registry_not_found (404)
  - test_json_schemas_collection (count≥20, ComputerSystem present)
  - test_get_json_schema_computer_system (Id, type, URI)
  - test_get_json_schema_not_found (404)

Lint: fix redundant_closure in websocket/mod.rs; remove unused `put`
import in dbus_rest.rs.  Zero clippy warnings.
134 unit tests passing.
